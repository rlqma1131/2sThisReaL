using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CampFire : MonoBehaviour
{
    //public int damage;
    //public float damageRate;

    ////List<IDamageable> things = new List<IDamageable>();
    //void Start()
    //{
    //    InvokeRepeating("DealDamage", 0, damageRate);   // 바로 시작해서 damageRate 간격으로 반복 호출
    //}

    //void DealDamage()
    //{
    //    for(int i = 0; i < things.Count; i++)
    //    {
    //        things[i].TakePhysicalDamage(damage);
    //    }
    //}

    //private void OnTriggerEnter(Collider other)
    //{
    //    if(other.TryGetComponent(out IDamageable damageable)) // IDamageable 컴포넌트를 갖고 있다면 충돌했을 때 things 리스트에 추가
    //    {
    //        things.Add(damageable);
    //    }
    //}

    //private void OnTriggerExit(Collider other)  // IDamageable 컴포넌트를 갖고 있다면 충돌이 끝났을 때 things 리스트에서 제거
    //{
    //    if(other.TryGetComponent(out IDamageable damageable))
    //    {
    //        things.Remove(damageable);
    //    }
    //}
}
